# FP-0095 - Read-only ChatGPT App MCP Premium UI Preview Route State Matrix Master Plan

## Purpose / Big Picture

Target phase: `V2O`.

Exact slice: `V2O-read-only-chatgpt-app-mcp-premium-ui-preview-route-state-matrix-master-plan-local-v1`.

Status: validated docs-and-plan plus proof-gate compatibility slice, ready for PR review as of 2026-05-10.

FP-0095 is not implementation. FP-0095 is docs-and-plan plus proof-gate compatibility only. FP-0095 is a local preview route state-matrix and premium visual QA master-plan only.

This is not route implementation. This is not public ChatGPT App implementation. This is not Apps SDK iframe/UI resource implementation. This is not remote MCP deployment. This is not OAuth. This is not app submission. This is not endpoint implementation. This is not OpenAI API/model integration. This is not deployment. This is not product runtime behavior.

FP-0095 does not authorize route code yet. FP-0095 does not authorize route/UI code changes yet. FP-0095 does not authorize public app implementation. FP-0095 does not authorize Apps SDK iframe/UI resources. FP-0095 does not authorize endpoint implementation. FP-0095 does not authorize remote MCP deployment. FP-0095 does not authorize OAuth implementation. FP-0095 does not authorize app submission.

This plan creates no product code, no route code, no app route, no API route, no backend route, no endpoint, no endpoint implementation, no remote MCP server, no Apps SDK iframe/UI resource registration, no OAuth, no app submission, no public app implementation, no schema, no migration, no package script, no smoke alias, no eval dataset, no fixture, no sample data, no public demo data, no public source pack, no source-pack mutation, no OpenAI API/model calls, no hosted tool, no vector/file-search integration, no OCR, no PageIndex, no provider/certification/deployment work, no external communication, no source mutation, no finance writes, no generated product prose, no runtime-Codex finance output, no autonomous action, no screenshot binaries, no generated image assets, no public listing assets, no app-submission assets, and no public assets.

The user-visible purpose is to make the next local UI route work precise before any state matrix route code exists. Future UI should be premium, calm, Apple/OpenAI-style, evidence-first, and polished before publication. FP-0095 plans how a later slice may extend the shipped FP-0094 local preview route with a future local read-only preview route state matrix and premium visual QA hardening, but only if that later slice explicitly opens real route/UI work before edits.

A future implementation slice may extend the existing local route only if it remains local, proof-only, read-only, and does not add API routes, backend routes, OAuth, remote MCP, Apps SDK iframe/resource registration, OpenAI API/model calls, provider/certification/deployment, source mutation, finance writes, autonomous action, public app assets, screenshot or generated image assets, or app submission artifacts.

Do not add the state matrix yet. Do not add screenshots.

Public ChatGPT App implementation must still wait. Public app implementation must still wait. Public app submission must still wait.

Authority model remains unchanged:

- raw sources remain authoritative for document claims
- the Finance Twin remains authoritative for structured finance facts
- the CFO Wiki remains compiled and derived
- EvidenceIndex remains the source anchor, document map, evidence card, source coverage, freshness, and limitation layer
- V2C tools remain local/internal read-only evidence contracts
- V2D Evidence Atlas remains the shipped read-only local operator UI foundation
- V2E remains local/internal proof-only bounded orchestration
- V2F remains docs/proof-only benchmark/community contracts with no datasets or real finance data
- V2G remains local proof-only read-only ChatGPT App/MCP contracts, descriptors, and response envelopes
- FP-0091 remains the shipped local/proof-only/read-only component foundation
- FP-0092 remains the shipped local/proof-only/read-only composition/accessibility foundation
- FP-0094 remains the shipped single local/proof-only/read-only preview route foundation

GitHub connector product behavior is explicitly out of scope. Routine `git`, `gh`, push, and PR operations for this repository do not invoke GitHub Connector Guard.

Replay and evidence-bundle implications: this slice creates no mission state transition, ingest action, report action, approval, durable product runtime output, source mutation, Finance Twin write, CFO Wiki write, evidence bundle, provider job, certification record, delivery record, endpoint, backend route, route implementation, or app/MCP runtime behavior. The proof-gate bridge is repo validation posture only.

## Progress

- [x] 2026-05-10T00:55:01Z - Invoked the requested Pocket CFO operator skills: Finance Plan Orchestrator, Modular Architecture Guard, Source Provenance Guard, CFO Wiki Maintainer, Evidence Bundle Auditor, F6 Monitoring Semantics Guard, Validation Ladder Composer, and Pocket CFO Handoff Auditor.
- [x] 2026-05-10T00:55:01Z - Confirmed GitHub Connector Guard is out of scope because GitHub connector product behavior is not part of this slice.
- [x] 2026-05-10T00:55:01Z - Ran preflight against fetched `origin/main` on branch `codex/v2o-read-only-chatgpt-app-mcp-premium-ui-preview-route-state-matrix-master-plan-local-v1`; the repo started clean, `HEAD` matched `origin/main`, GitHub auth/repo access worked, PR #255 was merged, Docker Postgres/MinIO were available, FP-0094 existed and shipped, FP-0095 was absent, FP-0096 was absent, and required V2 proof tools existed.
- [x] 2026-05-10T00:55:01Z - Read required active docs, shipped FP-0091 through FP-0094 records, plugin inventory, package metadata, existing read-only app/MCP components, preview route, app/web route and lib inventory, V2F/V2G domain contracts, proof tools, and active boundary docs before edits.
- [x] 2026-05-10T00:55:01Z - Ran baseline proof gates before edits; all passed and the V2F/V2G gates proved `fp0095Absent: true`, which is expected pre-edit state before this docs-only state-matrix and visual QA successor.
- [x] 2026-05-10T00:55:01Z - Created this FP-0095 plan as the only allowed FP-0095 file.
- [x] 2026-05-10T02:08:02Z - Updated the minimal V2F/V2G proof schemas, builders, proof tools, and focused specs so they accept exactly this docs-only FP-0095 successor while continuing to reject FP-0096 and route/runtime/public app scope.
- [x] 2026-05-10T02:08:02Z - Refreshed directly stale active docs and `plugins.md` so FP-0094 remains the latest shipped local preview route foundation and FP-0095 is recorded only as the docs-and-plan state-matrix and visual QA planning successor.
- [x] 2026-05-10T02:08:02Z - Ran focused validation after edits; all required proof gates and focused V2F/V2G specs passed.
- [x] 2026-05-10T02:08:02Z - Ran strict same-branch QA over changed paths and runtime-pattern searches; no app routes, API routes, backend routes, endpoints, schema/migrations, package scripts, eval datasets, fixtures, sample data, source packs, FP-0096, OpenAI API/model calls, OAuth, app submission artifacts, provider/deployment work, source mutation, finance writes, autonomous action, screenshots, images, public assets, or public app implementation were added.
- [x] 2026-05-10T01:14:33Z - Ran full final validation: `git diff --check`, all seven required proof gates, focused V2F/V2G specs, `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm ci:repro:current`; all passed. The post-closeout minimum validation set is required after this record update and before commit.

## Surprises & Discoveries

Baseline V2F/V2G gates still proved FP-0095 absence after shipped FP-0094. That is correct pre-edit behavior. This slice must replace the hard FP-0095 absence posture with an exact FP-0095 docs-only local preview route state-matrix and visual QA planning boundary while keeping FP-0096 absent.

FP-0094 post-merge QA already added route-level `robots` metadata with noindex, nofollow, and noarchive posture. FP-0095 keeps noindex/local-only metadata as a future acceptance requirement in case a later implementation changes route metadata or page structure.

No real finance data, public sample pack, eval dataset, fixture, sample data, source-pack mutation, write tools, OAuth implementation, app submission, remote endpoint implementation, OpenAI API/model calls, provider/certification/deployment/external communications work, package-scope rename, GitHub module deletion, engineering-twin module deletion, finance write, source mutation, screenshot assets, generated image assets, or public assets are required.

Official web/plugin research used in this slice:

- No new official web research was used.
- No OpenAI Developers tool was used to create API keys, call OpenAI APIs, call models, or widen app/runtime scope.
- No OpenAI API key was created or used.
- No OpenAI API/model call was made.
- No Build Web Apps, Codex Security, Figma, app-submission, design-generation, artifact-upload, dependency-installation, screenshot-generation, image-generation, public-asset, or upload plugin workflow was used.

## Decision Log

Decision: FP-0095 is safe to create only as docs-and-plan plus proof-gate compatibility.
Rationale: PR #255 is merged, FP-0094 is shipped as the single local preview route foundation, required proof gates passed before edits, and this work can stay non-runtime.

Decision: FP-0095 does not authorize route/UI code yet.
Rationale: a route state matrix is real route/UI work. A later implementation slice must tell the user clearly before edits that real route/UI work is starting.

Decision: FP-0095 defines future local preview route state-matrix and premium visual QA boundary only.
Rationale: the next safe step is to specify route states, noindex/local-only posture, and visual QA acceptance without adding route code, screenshots, public assets, app submission assets, OpenAI API/model calls, OAuth, remote MCP, or endpoint behavior.

Decision: future implementation must extend only the existing FP-0094 local route if it stays local/proof-only/read-only.
Rationale: FP-0094 already made FP-0091/FP-0092 observable in one local page. Future work should harden that route rather than adding more product runtime surfaces.

Decision: proof gates must accept exactly one docs-only FP-0095 file and reject FP-0096.
Rationale: replacing `fp0095Absent` with exact FP-0095 boundary fields is stronger than deleting the guard. FP-0096 remains absent and future-only.

Decision: no replay event is added.
Rationale: this slice changes planning records, active docs, and validation gates only. It does not change product runtime state, mission state, source state, reports, approvals, or evidence artifacts.

## Context and Orientation

FP-0091 shipped the first local/proof-only/read-only premium UI component foundation under `apps/web/components/read-only-app-mcp/**`. FP-0092 shipped the local/proof-only/read-only composition/accessibility hardening layer over those components. FP-0093 shipped the docs-only local preview route master-plan. FP-0094 shipped exactly one local/proof-only/read-only preview route at `apps/web/app/read-only-app-mcp-preview/page.tsx` and later added noindex/noarchive route metadata during post-merge QA.

FP-0095 plans the next future local read-only preview route state matrix and premium visual QA hardening. It does not add the state matrix yet. It does not add screenshots, generated images, public assets, app submission assets, endpoint behavior, public ChatGPT App implementation, Apps SDK iframe/UI resources, OAuth, remote MCP deployment, OpenAI API/model calls, provider/certification/deployment work, source mutation, finance writes, generated product prose, runtime-Codex finance output, or autonomous action.

Future state matrix must show:

- answer state
- missing citation refusal
- unsupported evidence refusal
- stale evidence refusal
- conflicting evidence refusal if available
- prompt-injection warning state
- raw full-file dump refusal state
- unsafe action refusal state
- empty evidence state
- loading evidence state
- error/unsupported state
- privacy/no-runtime boundary state

Future route tests must prove:

- no fetch
- no POST
- no form
- no buttons/action-looking forbidden controls
- no server action
- no API route
- no backend route
- no OpenAI API/model call
- no OAuth
- no app submission artifact
- no raw full-file dump panels
- no advice-like CTA copy
- no screenshot/image/public asset files

Future implementation must preserve noindex/local-only metadata. FP-0094 already exports static local-preview robots metadata; any future route change must keep that posture explicit.

## Plan of Work

Allowed files for this slice:

- `plans/FP-0095-read-only-chatgpt-app-mcp-premium-ui-preview-route-state-matrix-master-plan.md`
- `plans/FP-0094-read-only-chatgpt-app-mcp-premium-ui-preview-route-foundation.md` only for tiny shipped-state wording polish if directly stale
- `plugins.md` only if directly stale
- `README.md` only if directly stale
- `CODEX_README.md` only if directly stale
- `START_HERE.md` only if directly stale
- `docs/ACTIVE_DOCS.md` only if directly stale
- `docs/PROJECT_STATE.md` only if directly stale
- `docs/V2_BOUNDARY.md` only if directly stale
- `plans/ROADMAP.md` only if directly stale
- `packages/domain/src/read-only-app-mcp*.ts` only for proof-gate fields or helpers
- `packages/domain/src/benchmark-community*.ts` only for proof-gate fields or helpers
- `packages/domain/src/read-only-app-mcp*.spec.ts` only for proof-gate tests
- `packages/domain/src/benchmark-community*.spec.ts` only for proof-gate tests
- `tools/read-only-mcp-descriptor-response-envelope-proof.mjs`
- `tools/read-only-chatgpt-app-mcp-proof.mjs`
- `tools/benchmark-community-pack-proof.mjs`

Forbidden behavior:

- no route code
- no app routes
- no web API routes
- no backend/control-plane routes
- no endpoints
- no remote MCP server
- no Apps SDK iframe/UI resource registration
- no OAuth
- no app submission
- no schema or migrations
- no package scripts or smoke aliases
- no eval datasets, fixtures, sample data, public demo data, or source packs
- no source-pack fixture edits
- no FP-0096
- no public app implementation
- no OpenAI API/model calls
- no provider/certification/deployment/external communications
- no source mutation
- no finance writes
- no generated product prose or runtime-Codex finance output
- no autonomous action
- no screenshot binaries, generated images, public listing assets, app-submission assets, or public assets

Proof-gate bridge fields must prove:

- `fp0095AbsentOrDocsOnlyPreviewRouteStateMatrixBoundaryVerified`
- `fp0096Absent`
- `localPreviewRouteStateMatrixPlanBoundaryVerified`
- `noRouteImplementationFromFp0095`
- `noScreenshotAssetsFromFp0095`
- `noEndpointOauthSubmissionFromFp0095`
- `noPublicAppImplementationFromFp0095`
- `noAppsSdkIframeFromFp0095`
- `noRemoteMcpDeploymentFromFp0095`
- `noOpenAiApiModelCallsFromFp0095`
- `noProviderCertificationDeploymentFromFp0095`
- `noSourceMutationFinanceWriteFromFp0095`
- `noGeneratedProductProseRuntimeCodexFromFp0095`
- `noPublicAssetsFromFp0095`

## Concrete Steps

1. Run preflight and baseline proof gates.
2. Confirm baseline proof gates prove FP-0095 absence and treat that as expected pre-edit state.
3. Create this plan as the only FP-0095 file.
4. Update minimal V2F/V2G proof schemas, builders, proof tools, and focused specs so the exact docs-only FP-0095 local preview route state-matrix and visual QA master-plan is accepted while FP-0096 remains absent.
5. Refresh only directly stale active docs and `plugins.md` if required.
6. Run focused validation.
7. Run strict same-branch QA over changed files and patch only this slice if needed.
8. Run final validation.
9. If closeout docs are edited after validation, rerun the minimum post-closeout validation.
10. Commit exactly once, push the requested branch, and open the requested PR.

## Validation and Acceptance

Baseline proof gates run before edits:

```bash
pnpm exec tsx tools/read-only-mcp-descriptor-response-envelope-proof.mjs
pnpm exec tsx tools/read-only-chatgpt-app-mcp-proof.mjs
pnpm exec tsx tools/benchmark-community-pack-proof.mjs
pnpm exec tsx tools/bounded-llm-orchestration-proof.mjs
pnpm exec tsx tools/read-only-evidence-app-proof.mjs
pnpm exec tsx tools/document-precision-foundation-proof.mjs
pnpm exec tsx tools/evidence-index-foundation-proof.mjs
```

Required focused validation after edits:

```bash
pnpm exec tsx tools/read-only-mcp-descriptor-response-envelope-proof.mjs
pnpm exec tsx tools/read-only-chatgpt-app-mcp-proof.mjs
pnpm exec tsx tools/benchmark-community-pack-proof.mjs
pnpm exec tsx tools/bounded-llm-orchestration-proof.mjs
pnpm exec tsx tools/read-only-evidence-app-proof.mjs
pnpm exec tsx tools/document-precision-foundation-proof.mjs
pnpm exec tsx tools/evidence-index-foundation-proof.mjs
pnpm --filter @pocket-cto/domain exec vitest run src/benchmark-community.spec.ts src/read-only-app-mcp.spec.ts src/read-only-app-mcp-descriptor.spec.ts
```

Strict same-branch QA:

- Search changed files for route implementation, endpoint implementation, schema/migrations, package scripts, eval datasets, fixtures, sample data, source packs, OpenAI API/model calls, OAuth, app submission, provider/deployment, source mutation, finance writes, autonomous action, screenshots, images, public assets, and public app implementation.
- Confirm no app routes were added or edited.
- Confirm no API routes or backend routes were added.
- Confirm no screenshot binaries or generated image assets.
- Confirm no FP-0096.
- Patch on this same branch if QA finds a defect, then rerun required validation.

Required final validation:

```bash
git diff --check
pnpm exec tsx tools/read-only-mcp-descriptor-response-envelope-proof.mjs
pnpm exec tsx tools/read-only-chatgpt-app-mcp-proof.mjs
pnpm exec tsx tools/benchmark-community-pack-proof.mjs
pnpm exec tsx tools/bounded-llm-orchestration-proof.mjs
pnpm exec tsx tools/read-only-evidence-app-proof.mjs
pnpm exec tsx tools/document-precision-foundation-proof.mjs
pnpm exec tsx tools/evidence-index-foundation-proof.mjs
pnpm --filter @pocket-cto/domain exec vitest run src/benchmark-community.spec.ts src/read-only-app-mcp.spec.ts src/read-only-app-mcp-descriptor.spec.ts
pnpm lint
pnpm typecheck
pnpm test
pnpm ci:repro:current
```

Acceptance evidence:

- exactly one FP-0095 file exists at `plans/FP-0095-read-only-chatgpt-app-mcp-premium-ui-preview-route-state-matrix-master-plan.md`
- FP-0095 is docs-and-plan only
- FP-0095 plans a future local read-only preview route state-matrix and premium visual QA boundary only
- FP-0095 does not authorize route code, API route, backend route, endpoint, remote MCP server, Apps SDK iframe/resource registration, OAuth, app submission, provider/certification/deployment, OpenAI API/model calls, source mutation, finance writes, generated product prose, runtime-Codex finance output, autonomous action, public app implementation, screenshot binaries, generated image assets, or public assets
- FP-0096 remains absent
- existing FP-0094 local preview route foundation boundary remains intact
- existing FP-0093 preview-route plan boundary remains intact
- existing FP-0092 and FP-0091 local UI boundaries remain intact
- existing FP-0087 typed boundary remains intact
- public app implementation and public app submission remain future-only

## Idempotence and Recovery

Rerunning this slice should find this exact FP-0095 file and update it rather than creating another FP-0095 or FP-0096.

If proof gates fail because FP-0095 is present, patch only the proof-gate bridge so the exact docs-only local preview route state-matrix and visual QA planning boundary is accepted. Do not widen into route implementation, endpoint implementation, public app, Apps SDK iframe/UI resources, OAuth, submission, remote MCP, provider, deployment, OpenAI API/model, source mutation, finance-write behavior, screenshot assets, generated image assets, public assets, or app-submission artifacts.

If validation fails after a correction, report the exact failing command and recommend the smallest safer corrective slice:

- FP-0095 proof-gate bridge correction
- FP-0094 route boundary correction
- FP-0092 composition/accessibility correction
- FP-0091 component correction
- hold state-matrix planning until local route boundaries can be proven

## Artifacts and Notes

This plan records no new source files, sample data, fixtures, eval datasets, screenshots, generated images, public listing assets, app submission materials, app metadata, OAuth material, endpoints, routes, provider credentials, deployment artifacts, real finance data, or public source packs.

No source data was read, written, mutated, synthesized, uploaded, or exported. No mission-facing output was generated. No evidence bundle is created by this docs-only slice.

## Interfaces and Dependencies

Future route state-matrix implementation, if later authorized, must depend only on the existing FP-0094 local preview route, existing FP-0091/FP-0092 component exports under `apps/web/components/read-only-app-mcp/**`, and existing app/web styling patterns. It must not add API routes, backend routes, fetch calls, POST calls, forms, buttons or action-looking forbidden controls, server actions, OpenAI API/model calls, OAuth, app submission artifacts, raw full-file dump panels, advice-like CTA copy, screenshot binaries, generated image assets, public assets, or public app runtime behavior.

The future state matrix should remain synthetic and contract-shaped until a later plan opens a real data path. It must not create fixtures, sample data, eval datasets, public demo data, public source packs, source-pack mutations, source mutations, finance writes, generated product prose, runtime-Codex finance output, or autonomous actions.

No new environment variables, package scripts, smoke aliases, migrations, schema files, external services, OpenAI API keys, provider credentials, OAuth material, app-submission assets, or deployment artifacts are introduced by FP-0095.

## Outcomes & Retrospective

Outcome: FP-0095 exists as the only FP-0095 file and remains docs-and-plan plus proof-gate compatibility only. The proof-gate bridge accepts exactly this local preview route state-matrix and visual QA planning successor, keeps FP-0096 absent, and preserves the shipped FP-0091, FP-0092, FP-0093, and FP-0094 boundaries.

Validation outcome: final validation passed before this closeout edit. The minimum post-closeout validation set is `git diff --check`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm ci:repro:current` before commit.

Scope outcome remained: docs-and-plan plus proof-gate compatibility only; no route implementation, app routes, API routes, backend routes, endpoints, remote MCP server, Apps SDK iframe/UI resource registration, OAuth, app submission, schema, migration, package script, smoke alias, eval dataset, fixture, sample data, public demo data, source pack, OpenAI API/model call, vector/file-search, OCR, PageIndex, provider/certification/delivery/deployment, external communication, source mutation, finance write, generated product prose, runtime-Codex finance output, autonomous action, FP-0096, screenshots, generated images, public assets, or public app implementation.
